import fetch from 'node-fetch';

(async () => {
  // Login to get token
  // I don't know the exact credentials, so I will check the DB for a doctor and generate a token directly!
  console.log("We need a token...");
})();
